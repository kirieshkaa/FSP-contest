# Настройки сервера
HOST=127.0.0.1
PORT=8000

# Путь к файлу с эмпирическими параметрами (если используется params_source=empirical)
PARAMS_FILE=simulation_model.pkl

# Текущая дата для расчёта возраста (по умолчанию используется datetime.now())
CURRENT_DATE=2026-02-26